<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- MediaRouteButton crashes if windowBackground/colorPrimary is translucent (#0). -->
    <style name="CastMediaRouteButtonTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/playerToolbar</item>
        <item name="colorPrimaryDark">@color/playerToolbar</item>
        <item name="android:colorBackground">@color/playerToolbar</item>
        <item name="android:windowBackground">@color/playerToolbar</item>
    </style>
</resources>
